

# This creates the environment

conda install numpy pandas tensorflow
pip install transformers
pip install torch
conda install spyder


# Data name notes

validation_set_2.csv is 200 labled instances that is just used to validate model formation


Data_for_label_n.csv is produced by R. It is the data that is then hand lablled and saved as the same name.
This is used to train the model.

